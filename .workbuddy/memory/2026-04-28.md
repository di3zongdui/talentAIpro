# 2026-04-28 工作日志

## 修改记录

### TalentAI_Pro API配置修复

**问题**: 测试API时使用硬编码的测试API Key `sk-test`，导致所有API测试失败。

**修改文件**:

1. **TalentAI_Pro/frontend/workflow_dashboard.html**:
   - 添加 `DEFAULT_SILICONFLOW_CONFIG` 默认配置（API Key: sk-unpavlxvzzormhmxlpzlajqadfrgvybfzevprbjqphbzigtr, Model: Qwen/Qwen3-Omni-30B-A3B-Instruct）
   - 添加 `loadLLMConfig()` 函数，页面加载时自动填充配置到表单
   - 修改 `testAllAPIs()` 函数，从页面输入框或localStorage读取API Key和Model
   - 修复LLM对话请求体从 `{message, provider, model}` 改为 `{prompt, model}`

2. **TalentAI_Pro/llm/gateway.py**:
   - 修改 `get_model()` 方法，添加通过 `model_key` 备用查找逻辑
   - 支持前端发送完整 model_key（如 `Qwen/Qwen3-Omni-30B-A3B-Instruct`）而非 model_id（如 `qwen3-omni`）

**配置生效**: SiliconFlow API Key 已在 `configs/llm_providers.json` 中配置，无需修改。

---

### 强化谈判API - 多轮对话记忆

**目标**: 让薪资谈判支持多轮对话，LLM能记住历史上下文。

**新增功能**:
1. `NegotiationSession` 类 - 维护多轮对话记忆
2. 增强 `negotiation_message()` 方法 - 支持 `session_id` 和 `candidate_reply`
3. 新增端点:
   - `GET /api/llm/negotiation-session/{session_id}` - 获取会话历史
   - `DELETE /api/llm/negotiation-session/{session_id}` - 清除会话

**修改文件**:
- `TalentAI_Pro/llm/gateway.py` - 添加会话存储和记忆逻辑
- `TalentAI_Pro/api/llm_routes.py` - 更新Request/Response模型和新端点
- `TalentAI_Pro/README.md` - 添加谈判API文档

**测试方法**:
1. 首次调用: POST `/api/llm/negotiation-message` (无session_id)
2. 获取返回的 session_id
3. 多轮调用: POST `/api/llm/negotiation-message` (带session_id和candidate_reply)

**Bug修复**:
- `llm/gateway.py`: 添加 `import uuid` 和 `from datetime import datetime`（这两个模块被使用但未导入）

**测试验证** (2026-04-28 22:47):
- 首次谈判 → session_id: neg-6dbffe54ff3d, round: 0 ✅
- 多轮对话 → round: 1，会话记忆正常 ✅
- 获取会话历史 → 3条消息，时间戳正确 ✅

---

### 多Agent谈判 API（新建）

**新增文件**: `TalentAI_Pro/api/workflow_routes.py` (新增 multi-agent 端点)

**端点**: `POST /api/workflow/multi-agent/negotiate`

**功能**: 同时调用 RecruiterAgent 和 CandidateAgent，模拟双方博弈，生成：
- RecruiterAgent 策略分析
- CandidateAgent 策略分析
- 推荐offer方案
- 谈判成功率预测

**数据存储**: 写入 SQLite 数据库（negotiation_db）

---

### 智能面试评估 API（新建）

**新增文件**: `TalentAI_Pro/api/interview_routes.py`

**端点**:
- `POST /api/interview/intelligent-evaluation` - 智能评估
- `GET /api/interview/evaluation/{session_id}` - 获取结果
- `GET /api/interview/evaluations` - 列出记录

**功能**: 基于简历+职位+问答，生成个性化评估报告
- 5维度评分（技术/沟通/解决问题/文化匹配/潜力）
- 优势/劣势分析
- 建议追问
- 录用建议

---

## 今日测试验证总结

**API测试验证** (2026-04-28 23:24):
- 多Agent谈判 API → session_id: multi-neg-f0e04f0ab064 ✅
- 智能面试评估 API → session_id: interview-8732d6d47ebd, score: 75 ✅

**关键Bug修复**:
- `llm/gateway.py`: 当model未在registry中注册时，直接使用model_key调用provider
- `api/workflow_routes.py`: 改为从 `api.llm_routes` 导入 `get_gateway`（统一provider实例）
- `api/interview_routes.py`: 同样改为从 `api.llm_routes` 导入

---

### 前端UI扩展 (workflow_dashboard.html)

**新增Tab页面**:
1. **🤝 薪资谈判** - 多Agent谈判 + 多轮对话 + 会话管理
2. **📊 智能面试** - 评估表单 + 历史记录

**新增JavaScript函数**:
- `runMultiAgentNegotiation()` - 多Agent谈判
- `startNegotiation()` / `continueNegotiation()` - 多轮对话
- `loadNegotiationSessions()` - 加载会话列表
- `searchNegotiations()` / `viewNegotiation()` - 搜索和查看会话
- `runIntelligentEvaluation()` - 智能面试评估
- `loadEvaluationHistory()` - 评估历史

---

### Agent 协作关系图谱 (workflow_dashboard.html)

**新增内容**:
- 页面顶部新增"Agent 协作关系图谱"可视化区域
- 4个阶段展示招聘流程：职位发布 → 智能匹配 → 评估决策 → 入职
- 每个阶段显示对应的Agent节点，点击可打开操作面板
- 使用CSS flexbox实现横向流程图布局
- 连接线使用渐变色表示流程方向

**阶段划分**:
1. 📋 职位发布: Job Creator → Candidate Searcher
2. 🎯 智能匹配: Matcher → Interview Scheduler
3. 📊 评估决策: Evaluator → Negotiator
4. 🎉 入职: Onboarder

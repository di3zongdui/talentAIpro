# TalentAI Pro 授权数据获取架构

> **Version:** 1.0 | **Date:** 2026-04-23

---

## 1. 授权数据获取概述

```
┌─────────────────────────────────────────────────────────────────┐
│                   授权数据获取架构                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   用户 ──→ OAuth授权 ──→ 获取Token ──→ 拉取真实数据           │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    TalentAI Pro                          │  │
│   │                                                         │  │
│   │   ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  │  │
│   │   │ 猎聘    │  │ Boss直聘 │  │ 领英    │  │ 拉勾    │  │  │
│   │   │ Liepin  │  │ Boss    │  │LinkedIn │  │ Lagou   │  │  │
│   │   └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘  │  │
│   │        │            │            │            │        │  │
│   │   ┌────▼────────────▼────────────▼────────────▼────┐   │  │
│   │   │              OAuth Token Manager                │   │  │
│   │   │  • Token存储   • 自动刷新   • 有效期管理       │   │  │
│   │   └────────────────────┬───────────────────────────┘   │  │
│   │                        │                                │  │
│   │                        ▼                                │  │
│   │   ┌───────────────────────────────────────────────┐   │  │
│   │   │              Data Sync Engine                 │   │  │
│   │   │  • 简历数据   • 职位数据   • 公司数据         │   │  │
│   │   └───────────────────────────────────────────────┘   │  │
│   │                                                         │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 支持的平台和数据

### 2.1 平台授权矩阵

| 平台 | OAuth方式 | 简历数据 | 职位数据 | 公司数据 | 限制 |
|------|-----------|---------|---------|---------|------|
| **猎聘** | 官方API Key | ✅ 完整 | ✅ 完整 | ✅ 完整 | 需企业认证 |
| **Boss直聘** | Cookie模拟 | ✅ 完整 | ✅ 完整 | ✅ 部分 | 封号风险 |
| **领英** | OAuth 2.0 | ✅ 公开+人脉 | ❌ | ✅ 公开 | 需会员 |
| **拉勾** | OAuth 2.0 | ✅ 完整 | ✅ 完整 | ✅ 完整 | 需企业账号 |
| **脉脉** | OAuth 2.0 | ✅ 职场数据 | ✅ 部分 | ✅ 社交数据 | 需认证 |

### 2.2 获取的数据类型

```
┌─────────────────────────────────────────────────────────────────┐
│                      数据类型                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  👤 简历数据                    📋 职位数据                      │
│  ├── 基础信息（姓名/电话/邮箱）    ├── JD详情（职责/要求）       │
│  ├── 工作经历（公司/职位/时间）    ├── 薪资范围                   │
│  ├── 项目经历                    ├── 技能标签                     │
│  ├── 教育背景                    └── 猎头备注                    │
│  ├── 技能标签                                                     │
│  └── 自我评价                                                     │
│                                                                 │
│  🏢 公司数据                    🔗 社交关系                      │
│  ├── 公司基本信息                ├── 同事关系（领英）             │
│  ├── 估值/融资情况              ├── 同学关系（脉脉）             │
│  ├── 创始人团队                  └── 师兄师姐（脉脉）             │
│  ├── 业务范围                                                     │
│  └── 员工评价                                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. OAuth 授权流程

### 3.1 猎聘 OAuth 流程

```python
class LiepinOAuth:
    """猎聘OAuth授权"""

    AUTHORIZE_URL = "https://passport.liepin.com/cas/oauth2/authorize"
    TOKEN_URL = "https://passport.liepin.com/cas/oauth2/access_token"
    API_BASE = "https://api.liepin.com"

    def __init__(self, client_id: str, client_secret: str, redirect_uri: str):
        self.client_id = client_id
        self.client_secret = client_secret
        self.redirect_uri = redirect_uri

    def get_authorization_url(self) -> str:
        """获取授权URL"""
        params = {
            "client_id": self.client_id,
            "redirect_uri": self.redirect_uri,
            "response_type": "code",
            "scope": "resume_read job_read company_read"
        }
        return f"{self.AUTHORIZE_URL}?{urllib.parse.urlencode(params)}"

    async def exchange_code_for_token(self, code: str) -> dict:
        """用code换取access_token"""
        async with aiohttp.ClientSession() as session:
            async with session.post(
                self.TOKEN_URL,
                data={
                    "client_id": self.client_id,
                    "client_secret": self.client_secret,
                    "code": code,
                    "grant_type": "authorization_code",
                    "redirect_uri": self.redirect_uri
                }
            ) as resp:
                return await resp.json()
```

### 3.2 Token 管理器

```python
class TokenManager:
    """OAuth Token 管理器"""

    def __init__(self, db: Database):
        self.db = db
        self.tokens: Dict[str, TokenInfo] = {}

    async def save_token(self, user_id: str, platform: str, token_data: dict):
        """保存Token"""
        expires_at = datetime.now() + timedelta(seconds=token_data.get("expires_in", 7200))

        token = TokenInfo(
            user_id=user_id,
            platform=platform,
            access_token=token_data["access_token"],
            refresh_token=token_data.get("refresh_token"),
            expires_at=expires_at,
            scope=token_data.get("scope", "")
        )

        await self.db.tokens.upsert(token)
        self.tokens[f"{user_id}:{platform}"] = token

    async def get_valid_token(self, user_id: str, platform: str) -> str:
        """获取有效Token（自动刷新）"""
        key = f"{user_id}:{platform}"
        token = self.tokens.get(key)

        if not token:
            token = await self.db.tokens.find_one(user_id=user_id, platform=platform)

        if not token:
            raise AuthError(f"未授权 {platform}")

        # 检查是否需要刷新
        if token.expires_at - datetime.now() < timedelta(minutes=5):
            token = await self.refresh_token(token)
            self.tokens[key] = token

        return token.access_token
```

---

## 4. 授权管理前端

### 4.1 渠道授权页面

```
┌─────────────────────────────────────────────────────────────────┐
│                   📦 数据渠道授权                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  连接您的人脉和招聘平台，自动同步简历和职位数据                   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🦁 猎聘                                                │   │
│  │    企业认证招聘平台                                      │   │
│  │    [未连接]  [🔗 授权连接]                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🔗 领英                                                │   │
│  │    职场人脉网络                                         │   │
│  │    [未连接]  [🔗 授权连接]                               │   │
│  │    ⚠️ 需要领英会员权限                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📊 拉勾                                                │   │
│  │    互联网人才库                                         │   │
│  │    [未连接]  [🔗 授权连接]                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 授权状态显示

```
┌─────────────────────────────────────────────────────────────────┐
│                   ✅ 已授权渠道                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🦁 猎聘  ● 已连接                                        │   │
│  │    授权时间: 2026-04-23  │  Token剩余: 78天              │   │
│  │    数据概览: 简历 234份 │ 职位 12个                      │   │
│  │    最后同步: 10分钟前   │ [🔄 立即同步] [⚙️ 设置] [❌ 取消] │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🔗 领英  ● 已连接                                        │   │
│  │    授权时间: 2026-04-22  │  Token剩余: 45天              │   │
│  │    数据概览: 人脉 1,234  │ 公司 89家                      │   │
│  │    最后同步: 2小时前     │ [🔄 立即同步] [⚙️ 设置] [❌ 取消] │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 安全与合规

### 5.1 安全措施

```
┌─────────────────────────────────────────────────────────────────┐
│                      安全措施                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🔐 Token安全                                                   │
│  ├── Token加密存储（AES-256）                                   │
│  ├── Token不记录日志                                            │
│  ├── Token自动刷新机制                                          │
│  └── Token泄漏自动撤销                                          │
│                                                                 │
│  🔒 数据安全                                                    │
│  ├── 数据传输加密（HTTPS）                                      │
│  ├── 敏感字段脱敏（手机号/身份证）                              │
│  └── 访问审计日志                                               │
│                                                                 │
│  ⚖️ 合规要求                                                    │
│  ├── 用户明确授权                                               │
│  ├── 仅获取必要权限                                             │
│  └── 用户数据删除权                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. 总结

```
┌─────────────────────────────────────────────────────────────────┐
│                    授权数据获取架构                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🎯 目标：获取用户在各大招聘/人脉平台的真实数据                  │
│                                                                 │
│  📋 核心组件：                                                  │
│  ├── OAuth 授权管理器 ── 安全的 Token 管理                      │
│  ├── 平台插件 ── 每个平台独立的插件                            │
│  ├── 数据同步引擎 ── 增量同步 + 断点续传                        │
│  └── 授权管理前端 ── 用户授权 UI                                │
│                                                                 │
│  🔄 数据流：                                                    │
│  用户授权 → 获取Token → 定时拉取数据 → 标准化 → AI分析          │
│                                                                 │
│  ⚠️ 注意事项：                                                  │
│  • 各平台API限制不同（需会员/企业认证）                         │
│  • Token需定期刷新                                              │
│  • 遵守平台开发者协议                                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

*Document Version: 1.0 | Last Updated: 2026-04-23*
